- name: Get running processes list from remote host
  shell: "ps -few | grep '/bin/sh /home/reg/go' | awk '{print $2}'"
  register: running_processes

- name: Kill process ARS in POS
  ignore_errors: yes
  hell: "kill {{ item }}"
  with_items: "{{ running_processes.stdout_lines }}"
